const restaurantCollection = require("../models/restaurant.js")

module.exports = {
    getRestaurant : async (req,res) => {
        //TODO : Fetch restaurant from database
    },

    addRestaurant : (req,res) => {
        //TODO : Add Restuarant to database (Register)
    },        

    updateMenu : (req, res) => {
        //TODO : Update the menu field of that restaurant
    }
}