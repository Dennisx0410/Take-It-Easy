const customerCollection = require("../models/customer")

module.exports = {
    getOrder : async (req,res) => {
        //TODO : Fetch Order from database
    },

    addOrder : (req,res) => {
        //TODO : Add Order to database (Register)
    }        

    
}