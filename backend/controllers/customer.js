const customerCollection = require("../models/customer.js")

module.exports = {
    getCustomer : async (req,res) => {
        //TODO : Fetch user from database
    },

    addCustomer : (req,res) => {
        //TODO : Add Customer to database (Register)
    }  


}